<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @vote.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @vote.content %>
</p>

<%= link_to 'Edit', edit_vote_path(@vote) %> |
<%= link_to 'Back', votes_path %>
